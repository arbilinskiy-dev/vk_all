/**
 * Секция «Анатомия интерфейса» + «Структура данных участника»
 * Описание 4 зон экрана и интерфейса SystemListSubscriber
 */
import React from 'react';
import { Sandbox } from '../../shared';
import { MembersTableAnatomy } from '../ListsMocks';

export const InterfaceAnatomySection: React.FC = () => (
    <>
        {/* ===================================================================== */}
        {/* АНАТОМИЯ ИНТЕРФЕЙСА */}
        {/* ===================================================================== */}
        <section>
            <h2>Из чего состоит интерфейс</h2>

            <p>
                Экран просмотра участников делится на 4 логические зоны: панель фильтров сверху, 
                заголовки колонок таблицы, строки данных участников, индикатор загрузки следующей страницы внизу.
            </p>

            <Sandbox
                title="Структура интерфейса просмотра"
                description="Цветные рамки показывают где какая зона расположена"
                height="auto"
            >
                <MembersTableAnatomy />
            </Sandbox>

            <h3>Зона 1: Панель фильтров</h3>

            <p>
                Верхняя панель с поисковым полем и выпадающими списками. Здесь вы настраиваете отображение: 
                ищете конкретного человека по имени или ID, фильтруете по статусу (активные/забаненные/удалённые), 
                полу, времени последнего онлайна, платформе (iOS/Android/Web), возрасту, месяцу рождения.
            </p>

            <h3>Зона 2: Заголовки колонок</h3>

            <p>
                Серая шапка таблицы с названиями колонок. Она <strong>прилипает к верху экрана</strong> при прокрутке 
                (sticky header) — вы всегда видите что означает каждая колонка, даже если прокрутили на 1000 участника вниз.
            </p>

            <h3>Зона 3: Строки участников</h3>

            <p>
                Основная область с данными. Каждая строка — один человек. При наведении курсора строка подсвечивается 
                лёгким серым фоном — видно на кого вы смотрите.
            </p>

            <h3>Зона 4: Индикатор загрузки</h3>

            <p>
                Крутящийся спиннер внизу таблицы. Появляется когда система подгружает следующую порцию участников. 
                Это часть технологии «бесконечной прокрутки» — не нужно нажимать кнопки «Показать ещё», всё загружается автоматически.
            </p>
        </section>

        {/* ===================================================================== */}
        {/* СТРУКТУРА ДАННЫХ УЧАСТНИКА */}
        {/* ===================================================================== */}
        <section>
            <h2>Какие данные хранятся об участнике</h2>

            <p>
                Для каждого человека в списке система собирает 18 параметров из VK API. Вот полная структура:
            </p>

            <div className="not-prose bg-gray-900 text-gray-100 rounded-lg p-4 my-6 overflow-x-auto text-sm font-mono">
                <pre>{`interface SystemListSubscriber {
    id: string;                  // Внутренний ID в базе данных
    vk_user_id: number;          // ID пользователя ВКонтакте
    first_name: string;          // Имя
    last_name: string;           // Фамилия
    domain?: string;             // Короткая ссылка (annasmirn)
    photo_url?: string;          // Ссылка на аватар
    sex?: number;                // 1 - женский, 2 - мужской
    bdate?: string;              // Дата рождения "15.3.1995"
    city?: string;               // Город
    country?: string;            // Страна
    has_mobile?: boolean;        // Известен номер телефона
    last_seen?: number;          // Последний онлайн (Unix timestamp)
    platform?: number;           // 1-m.vk, 2-iOS, 3-iPad, 4-Android, 7-Web
    added_at: string;            // Когда добавлен в список (ISO дата)
    deactivated?: string;        // "banned" | "deleted"
    is_closed?: boolean;         // Закрытый профиль
    source: string;              // Откуда: "callback" | "conversation" | "posts_sync" | "manual"
}`}</pre>
            </div>

            <div className="bg-amber-50 border border-amber-200 rounded-lg p-4 my-6">
                <div className="flex items-start gap-3">
                    <svg className="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                        <path fillRule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clipRule="evenodd" />
                    </svg>
                    <div>
                        <h4 className="text-sm font-bold text-amber-900 mb-1">Почему некоторые поля пустые?</h4>
                        <p className="text-sm text-amber-800 mb-0">
                            Пользователи ВКонтакте могут скрывать информацию в настройках приватности. Если поле пустое — 
                            значит человек закрыл его от всех или от сообществ. Мы показываем только то, что доступно через API.
                        </p>
                    </div>
                </div>
            </div>
        </section>
    </>
);
