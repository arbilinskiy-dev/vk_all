# Шаблон документации бэкенд-фичи

## Формат файла: `.md.txt`
## Размещение: `docs/backend/features/<название>/`

---

```markdown
# <Название фичи>

## Обзор

Краткое описание: что делает, для чего нужна, основные сценарии использования.

## Архитектура

### Файлы модуля

| Файл | Назначение |
|------|------------|
| `routers/<name>.py` | API-эндпоинты |
| `services/<name>/` | Бизнес-логика |
| `crud/<name>_crud.py` | Операции с БД |
| `schemas/<name>.py` | Pydantic-схемы |

### Зависимости

- Какие другие модули использует
- Внешние API (VK API, Google AI и т.д.)

## API-эндпоинты

### `POST /api/<path>`

**Описание:** Что делает.

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|:------------:|----------|
| `name` | string | ✅ | Название |
| `project_id` | int | ✅ | ID проекта |
| `description` | string | ❌ | Описание |

**Пример запроса:**
```json
{
  "name": "Пример",
  "project_id": 1
}
```

**Ответ (200):**
```json
{
  "id": 42,
  "name": "Пример",
  "project_id": 1,
  "created_at": "2026-02-19T12:00:00"
}
```

**Ошибки:**
- `422` — невалидные данные
- `404` — не найдено
- `401` — не авторизован

### `GET /api/<path>`

...

## Бизнес-логика

### Основной поток

1. Получаем запрос через роутер
2. Валидация через Pydantic-схему
3. Бизнес-логика в сервисе
4. Запись/чтение через CRUD
5. Возврат результата

### Особенности и edge cases

- Описание нетривиальных решений
- Ограничения VK API (если есть)
- Обработка ошибок

## Модели БД

### Таблица: `<table_name>`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | Integer, PK | ID записи |
| `name` | String | Название |
| ... | ... | ... |

## История изменений

| Дата | Изменение |
|------|-----------|
| 2026-02-19 | Создана документация |
```
