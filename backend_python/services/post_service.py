# Этот сервис выступает в роли "хаба", импортируя и ре-экспортируя
# функции из более специализированных сервисных модулей. Это позволяет
# сохранить единую точку входа для роутеров (`import services.post_service`),
# одновременно улучшая организацию и читаемость кода.
# Использование `import *` здесь является осознанным архитектурным решением
# для реализации паттерна "Фасад" (Facade).

from .post_retrieval_service import *
from .post_actions_service import *
# vk_callback_service удалён — вся логика перенесена в services.vk_callback (модульная архитектура v2)